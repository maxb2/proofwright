# Wiki schema

Canonical concept pages this wiki maintains:

- [[page-a]]
- [[page-b]]

Every claim cites a graded source in `raw/`.

## Tooling

Run `pw check` after every ingest, `pw index --write` to rebuild the index, and
`pw search "<query>"` to retrieve pages.
